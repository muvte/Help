#include <iostream>
using namespace std;
void Ur (int *a, int *b, int *c, int *d, int *e, int *f)
{
	int x, y;
	if (a = d) {
		return 0;
		break;
	}
	y = ( (d * c) - (a * f) ) / ( (b * d) + (a * e) );
	x = ( c - (b * y) ) / a;
	
	
}
int main ()
{
	int a, b, c, d, e, f, res, resu, *X, *Y;
	cin >> a >> b >> c >> d >> e >> f;
	Ur (&a, &b, &c, &d, &e, &f);
	res = *X;
	resu = *Y;
	cout << "(" << res << " ; " << resu << ")";
}
